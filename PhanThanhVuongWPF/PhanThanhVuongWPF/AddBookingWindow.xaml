<Window x:Class="PhanThanhVuongWPF.AddBookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhanThanhVuongWPF"
        mc:Ignorable="d"
        Title="Book a Room" Width="500" SizeToContent="Height" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Style for TextBox controls -->
        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Margin" Value="0,4,0,2"/>
        </Style>
        
        <!-- Style for ComboBox -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="8,6,5,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Margin" Value="0,4,0,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <!-- Style for DatePicker -->
        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>        
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Margin" Value="0,4,0,2"/>
        </Style>
        
        <!-- Style for Labels -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10,10,6"/>
        </Style>
        
        <!-- Style for Buttons -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
        </Style>
        
        <Style x:Key="SaveButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E7EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Style for error messages -->
        <Style x:Key="ErrorMessageStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E53E3E"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>
    
    <Border Background="#F8F9FA" CornerRadius="0">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <TextBlock Grid.Row="0" Text="Book a Room" 
                       FontSize="20" FontWeight="Bold" 
                       Foreground="#1E3A8A" 
                       Margin="0,0,0,20" 
                       HorizontalAlignment="Center"/>
            
            <!-- Main Form -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="8" 
                    BorderBrush="#E2E8F0" 
                    BorderThickness="1">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Select Room -->
                    <TextBlock Text="Select Room:" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <ComboBox x:Name="RoomSelect" Style="{StaticResource ComboBoxStyle}" 
                                  DisplayMemberPath="Display" 
                                  SelectedValuePath="RoomID"
                                  SelectionChanged="RoomSelect_SelectionChanged"/>
                        <TextBlock x:Name="RoomSelectError" Style="{StaticResource ErrorMessageStyle}" Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- Room Details -->
                    <TextBlock Text="Room Details:" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="1" Grid.Column="1">
                        <TextBox x:Name="RoomDetails" Style="{StaticResource InputBoxStyle}" 
                                 IsReadOnly="True" Background="#F9FAFB" Height="70" 
                                 TextWrapping="Wrap" AcceptsReturn="True" 
                                 VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>

                    <!-- Start Date -->
                    <TextBlock Text="Start Date:" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <DatePicker x:Name="StartDate" Style="{StaticResource DatePickerStyle}" 
                                    SelectedDateChanged="StartDate_SelectedDateChanged"/>
                        <TextBlock x:Name="StartDateError" Style="{StaticResource ErrorMessageStyle}" Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- End Date -->
                    <TextBlock Text="End Date:" Grid.Row="3" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="3" Grid.Column="1">
                        <DatePicker x:Name="EndDate" Style="{StaticResource DatePickerStyle}" 
                                    SelectedDateChanged="EndDate_SelectedDateChanged"/>
                        <TextBlock x:Name="EndDateError" Style="{StaticResource ErrorMessageStyle}" Visibility="Collapsed"/>
                    </StackPanel>
                    
                    <!-- Price Estimate -->
                    <TextBlock Text="Price Estimate:" Grid.Row="4" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="4" Grid.Column="1">
                        <TextBox x:Name="PriceEstimate" Style="{StaticResource InputBoxStyle}" 
                                 IsReadOnly="True" Background="#F9FAFB"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,20,0,0">
                <Button Content="Book" 
                        Style="{StaticResource SaveButtonStyle}" 
                        Click="Save_Click" />
                <Button Content="Cancel" 
                        Style="{StaticResource CancelButtonStyle}" 
                        Click="Cancel_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
