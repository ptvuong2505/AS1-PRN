<Window x:Class="PhanThanhVuongWPF.AddCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhanThanhVuongWPF"
        mc:Ignorable="d"
        Title="Add Customer" Width="450" SizeToContent="Height" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Style chung cho các TextBox -->
        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Margin" Value="0,4,0,2"/>
        </Style>
        
        <!-- Style cho PasswordBox -->
        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Margin" Value="0,4,0,2"/>
        </Style>
        
        <!-- Style cho DatePicker -->
        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>        
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Margin" Value="0,4,0,2"/>
        </Style>
        
        <!-- Style cho Label -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10,10,6"/>
        </Style>
        
        <!-- Style cho các Button -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
        </Style>
        
        <Style x:Key="SaveButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E7EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Style cho error message -->
        <Style x:Key="ErrorMessageStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E53E3E"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>
    
    <Border Background="#F8F9FA" CornerRadius="0">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <TextBlock Grid.Row="0" Text="Add New Customer" 
                       FontSize="20" FontWeight="Bold" 
                       Foreground="#1E3A8A" 
                       Margin="0,0,0,20" 
                       HorizontalAlignment="Center"/>
            
            <!-- Main Form -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="8" 
                    BorderBrush="#E2E8F0" 
                    BorderThickness="1">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Full Name -->
                    <TextBlock Text="Full Name:" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBox x:Name="FullName" Style="{StaticResource InputBoxStyle}"/>
                        <TextBlock x:Name="FullNameError" Style="{StaticResource ErrorMessageStyle}" Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- Telephone -->
                    <TextBlock Text="Telephone:" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="1" Grid.Column="1">
                        <TextBox x:Name="Telephone" Style="{StaticResource InputBoxStyle}"/>
                        <TextBlock x:Name="TelephoneError" Style="{StaticResource ErrorMessageStyle}" Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- Email -->
                    <TextBlock Text="Email:" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <TextBox x:Name="Email" Style="{StaticResource InputBoxStyle}"/>
                        <TextBlock x:Name="EmailError" Style="{StaticResource ErrorMessageStyle}" Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- Password -->
                    <TextBlock Text="Password:" Grid.Row="3" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Grid.Row="3" Grid.Column="1">
                        <PasswordBox x:Name="Password" 
                                     Style="{StaticResource PasswordBoxStyle}"
                                     PasswordChanged="Password_PasswordChanged" />
                        <TextBlock x:Name="PasswordError" 
                                   Style="{StaticResource ErrorMessageStyle}" 
                                   Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- Birthday -->
                    <TextBlock Text="Birthday:" Grid.Row="4" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <DatePicker x:Name="Birthday" 
                                Grid.Row="4" Grid.Column="1" 
                                Style="{StaticResource DatePickerStyle}"/>
                </Grid>
            </Border>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,20,0,0">
                <Button Content="Save" 
                        Style="{StaticResource SaveButtonStyle}" 
                        Click="Save_Click" />
                <Button Content="Cancel" 
                        Style="{StaticResource CancelButtonStyle}" 
                        Click="Cancel_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
