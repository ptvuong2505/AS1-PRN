<Window x:Class="PhanThanhVuongWPF.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhanThanhVuongWPF"
        mc:Ignorable="d"
        Title="Admin Dashboard" Height="650" Width="900" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Style cho các button -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#EF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="#3B82F6"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Style cho DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F3F4F6"/>
            <Setter Property="VerticalGridLinesBrush" Value="#F3F4F6"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>
        
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#3B82F6" Padding="20,15">
            <Grid>
                <TextBlock Text="Hotel Management System" Foreground="White" FontSize="20" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Admin Dashboard" Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="Logout" 
                            Command="{Binding LogoutCommand}" 
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            Style="{StaticResource ActionButton}" 
                            Background="#2563EB"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Content -->
        <TabControl Grid.Row="1" Margin="20" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource TabItemStyle}"/>
            </TabControl.Resources>
            
            <!-- Tab khách hàng -->
            <TabItem Header="Customers">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Toolbar -->
                    <Border Grid.Row="0" Background="White" Padding="0,10,0,15">
                        <Grid>
                            <Button Content="Add Customer" Command="{Binding AddCustomerCommand}" 
                                    Style="{StaticResource ActionButton}" HorizontalAlignment="Left"/>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBox x:Name="SearchCustomerBox" Width="250" Style="{StaticResource SearchBox}" 
                                         Text="{Binding CustomerSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Content="Search" Command="{Binding SearchCustomersCommand}" 
                                        CommandParameter="{Binding ElementName=SearchCustomerBox, Path=Text}" 
                                        Style="{StaticResource ActionButton}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- DataGrid khách hàng -->
                    <DataGrid ItemsSource="{Binding Customers}" AutoGenerateColumns="False" Grid.Row="1" 
                              IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="60"/>
                            <DataGridTextColumn Header="Full Name" Binding="{Binding CustomerFullName}" Width="180"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" Width="180"/>
                            <DataGridTextColumn Header="Telephone" Binding="{Binding Telephone}" Width="120"/>
                            <DataGridTextColumn Header="Birthday" Binding="{Binding CustomerBirthday, StringFormat={}{0:dd/MM/yyyy}}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit" Command="{Binding DataContext.UpdateCustomerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource ActionButton}"/>
                                            <Button Content="Delete" Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource DeleteButton}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <!-- Tab phòng -->
            <TabItem Header="Rooms">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Toolbar -->
                    <Border Grid.Row="0" Background="White" Padding="0,10,0,15">
                        <Grid>
                            <Button Content="Add Room" Command="{Binding AddRoomCommand}" 
                                    Style="{StaticResource ActionButton}" HorizontalAlignment="Left"/>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBox x:Name="SearchRoomBox" Width="250" Style="{StaticResource SearchBox}"
                                         Text="{Binding RoomSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Content="Search" Command="{Binding SearchRoomsCommand}" 
                                        CommandParameter="{Binding ElementName=SearchRoomBox, Path=Text}" 
                                        Style="{StaticResource ActionButton}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- DataGrid phòng -->
                    <DataGrid ItemsSource="{Binding Rooms}" AutoGenerateColumns="False" Grid.Row="1" 
                              IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding RoomID}" Width="60"/>
                            <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="120"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding RoomDescription}" Width="250"/>
                            <DataGridTextColumn Header="Price" Binding="{Binding RoomPricePerDate, StringFormat={}{0:N0} đ}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding RoomStatus}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit" Command="{Binding DataContext.UpdateRoomCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource ActionButton}"/>
                                            <Button Content="Delete" Command="{Binding DataContext.DeleteRoomCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource DeleteButton}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <!-- Tab báo cáo -->
            <TabItem Header="Reports">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Toolbar báo cáo -->
                    <Border Grid.Row="0" Background="White" Padding="0,10,0,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="Start Date:" Margin="5" VerticalAlignment="Center"/>
                                <DatePicker SelectedDate="{Binding StartDate, ValidatesOnExceptions=True}" Margin="5" Width="150"/>
                                <TextBlock Text="End Date:" Margin="15,5,5,5" VerticalAlignment="Center"/>
                                <DatePicker SelectedDate="{Binding EndDate, ValidatesOnExceptions=True}" Margin="5" Width="150"/>
                                <Button Content="Generate Report" Command="{Binding GenerateReportCommand}" 
                                        Style="{StaticResource ActionButton}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- DataGrid báo cáo -->
                    <DataGrid ItemsSource="{Binding Bookings}" AutoGenerateColumns="False" Grid.Row="1" 
                              IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Booking ID" Binding="{Binding BookingID}" Width="80"/>
                            <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="150"/>
                            <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="100"/>
                            <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat={}{0:dd/MM/yyyy}}" Width="100"/>
                            <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat={}{0:dd/MM/yyyy}}" Width="100"/>
                            <DataGridTextColumn Header="Duration (days)" Binding="{Binding Duration}" Width="110"/>
                            <DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice, StringFormat={}{0:N0} đ}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Tổng kết -->
                    <Border Grid.Row="2" Background="#F9FAFB" Padding="10" BorderThickness="1,0,1,1" BorderBrush="#E5E7EB">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Total Bookings:" FontWeight="SemiBold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding TotalBookings}" Margin="0,0,15,0"/>
                            <TextBlock Text="Total Revenue:" FontWeight="SemiBold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding TotalRevenue, StringFormat={}{0:N0} đ}" Foreground="#047857"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
